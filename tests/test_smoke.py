def test_smoke():
    """Minimal smoke test to ensure the test runner runs successfully.
    This test does not import GUI or Scapy components to avoid requiring heavy deps in CI.
    """
    assert True
